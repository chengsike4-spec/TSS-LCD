# TSS-LCD

This repository contains the code for a temporal-spectral-spatial latent conditional diffusion model for wireless spectrum prediction.

## Structure

- `data/` : scripts or notes for dataset
- `models/` : model definitions
- `train.py` : training entry
- `test.py` : evaluation scripts
